{
  "fixture_id": "nonstandard-rootfile",
  "category": "nonstandard package layout",
  "description": "The package uses a nondefault rootfile path but remains engine-openable and validator-compatible.",
  "scenarios": [
    {
      "id": "replace-token-in-nonstandard-rootfile",
      "description": "A nondefault rootfile package still supports explicit token replacement and validation.",
      "operation": {
        "type": "token_replace",
        "scopes": ["body"],
        "search": "{{ROOT}}",
        "replacement": "ROOT0001"
      },
      "assertions": {
        "expected_replacements": 1,
        "page_guard_pass": true,
        "metric_fields_unchanged": [
          "section_count",
          "paragraph_count",
          "table_count"
        ],
        "body_contains": ["ROOT0001"],
        "body_absent": ["{{ROOT}}"]
      }
    }
  ]
}
