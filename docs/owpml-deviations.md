# OWPML 편차 레지스트리

공식 OWPML 스키마(de jure)와 한컴오피스 실동작(de facto) 사이의 확인된
편차와 호환 전략을 기록한다. 스키마 검증은 하드 게이트가 아니라 수렴 lint다.
확인된 편차는 로컬 `_schemas`를 한컴 현실에 맞게 패치할 때 근거가 된다.

## 네임스페이스 정합 (2011/2016 ↔ 2024)

- 구현 전략: `hwpx.oxml.namespaces`가 2011/2016/2024 OWPML namespace
  registry의 SSOT다. 읽기 경로는 namespace URI를 단일 2011 값으로 가정하지
  않고 local-name traversal과 registry helper로 2011/2016/2024 입력을
  수용한다.
- 쓰기 전략: 기존 문서를 편집할 때 paragraph/run/text 계층은 source element의
  namespace를 따라 새 element를 만든다. 따라서 2011/2016/2024 입력 문서는
  `HwpxDocument.open()` 후 `to_bytes()`에서 해당 문서 namespace를 보존한다.
- 신규 생성 전략: 현 코퍼스와 `hancom-io/hwpx-owpml-model` current HEAD가
  모두 2011 본체 namespace와 2016 확장 namespace를 사용하므로, 신규
  `HwpxDocument.new()` skeleton은 2011 본체 namespace를 유지한다.
- 코퍼스 증거: `tests/fixtures/hwpxlib_corpus/manifest.json`의 47개 샘플은
  `Contents/header.xml`과 `Contents/section0.xml` root 선언 기준 모두
  2011 본체 namespace + 2016 확장 namespace(`hp10`, `HwpUnitChar`) 조합이다.
  2024 namespace 실문서 샘플은 이 코퍼스에 없으므로 합성 fixture로 회귀
  테스트한다.

## 확인된 편차

각 항목은 4필드로 기록한다 — **스키마 주장**(`DevDoc/OWPML SCHEMA/*.xml`이
선언한 것) · **실문서 실측**(hwpxlib 코퍼스 또는 명시된 다른 코퍼스에서
관찰한 것) · **우리 처리**(코드가 실제로 어느 쪽을 따르는지, 파일:함수) ·
**프로브**(`probes/`의 재실행 가능한 스크립트 — 실행하면 실측과 우리 처리를
둘 다 다시 검증한다). 근거 파일이 `.gitignore` 대상(사설 코퍼스)이면 항목에
명시하고, 프로브는 그 파일이 없을 때 SKIP으로 정직하게 물러난다.

| ID | 스키마 주장 | 실문서 실측 | 우리 처리 | 프로브 | 상태 |
|---|---|---|---|---|---|
| DEV-001 | 2024 네임스페이스 중심 스키마 | 2011 본체 + 2016 확장 네임스페이스 문서가 hwpxlib 47개 코퍼스와 hancom-io/hwpx-owpml-model current HEAD에서 실사용됨 | `hwpx.oxml.namespaces`가 2011/2016/2024 registry SSOT — 읽기는 local-name traversal, 신규 생성은 2011 본체+2016 확장 유지 | (없음 — 최초 항목, 후속 프로브 전환 대상) | implemented |
| DEV-002 | `hh:tabPr`의 `hh:tabItem` 자식은 `minOccurs="0"`만 선언되고 `maxOccurs` 생략 — XSD 기본값 1이라 tabPr 하나에 tabItem 최대 1개 | `error__20240626__no_manifest.hwpx`(vendored) 한 `hh:tabPr`에 **31개** `hh:tabItem`. 사설 코퍼스(국세청 공문, `.gitignore` 대상)에서도 4개 독립 관찰 — 다른 생성 워크플로에서도 재현 | `hwpx.oxml.header.TabDefinition.tab_stops`가 무제한 리스트, `HwpxOxmlHeader.ensure_tab_definition`(header_part.py)이 순서까지 dedupe 키로 취급 | `probes/dev002_tabitem_maxoccurs.py` | implemented (6.1 트레인③, 커밋 a144733) |
| DEV-003 | `AbstractDrawingObjectType` 시퀀스: `lineShape, fillBrush, drawText, shadow` (선언 순서) | `reader_writer__SimpleRectangle.hwpx`(vendored) `hp:rect`: `lineShape, fillBrush, shadow, drawText` — shadow가 drawText보다 먼저 | `objects.py::_write_draw_text`가 "shadow 다음"이 아니라 "지오메트리(pt0류) 앞"에 앵커 — `_reposition_child_before_any`로 사이드 존재 여부에 안 기댐 | `probes/dev003_drawtext_child_order.py` | implemented (6.1 트레인⑤, 커밋 3d4e038) |
| DEV-004 | 없음 — `DevDoc/OWPML SCHEMA/`에 Settings 스키마 자체가 없다(7종 벤더링: Header/Body/Core/History/MasterPage/ParaList/Version) | `settings.xml`(`ha:HWPApplicationSetting`)이 코퍼스 100%에 존재. `config:config-item-set`/`config-item`은 Hancom 고유가 아니라 **OASIS ODF 1.0 config 스키마**(`urn:oasis:...:config:1.0`) 재사용 | `hwpx.oxml.settings`(신규 모듈) — 스키마 없이 실문서 177파일 역설계, config-item 이름·타입을 하드코딩 없이 보존 | `probes/dev004_settings_xml_no_schema.py` | implemented (6.1 트레인④, 커밋 c38bf07) |
| DEV-005 | `hh:layoutCompatibility`가 48개 플래그 이름을 전부 선언(`minOccurs="0"` 마커 자식) | 도달 가능한 코퍼스 전수(47/47) `<hh:layoutCompatibility/>` — 플래그 0개. 완전성 감사 §4-R1이 "코드가 단어조차 모르는" 유일 전수 요소로 지목했던 자리 | `hwpx.oxml.header.LayoutCompatibility.flags: frozenset[str]` — 48종 하드코딩 열거 대신 실제 존재하는 자식 이름만 보존(스키마 밖 미래 플래그도 무손실) | `probes/dev005_layout_compatibility_empty.py` | implemented (6.1 트레인④, 커밋 c38bf07) |
| DEV-006 | `hh:font/@isEmbedded`·`hh:tabPr/@autoTab*`는 `xs:boolean`(XSD 어휘상 true/false와 1/0 둘 다 유효) | 코퍼스 전수(6741건 font/substFont, 142건 tabPr) `"0"`/`"1"`만 관측 — `"true"`/`"false"` 0건(다른 OWPML 불리언 관행과 이원). 비임베드 `hh:font`는 `binaryItemIDRef` 속성 자체가 없고(1682/1682), `hh:substFont`는 항상 있되 빈 문자열로 남는다(284/284) | `_document_primitives._zero_one_bool_str`(범용 `_bool_str`과 분리) + `_build_font_element`의 조건부 `binaryItemIDRef` | `probes/dev006_fontface_write_conventions.py` | implemented (6.1 트레인②, 커밋 7914b18) |
| DEV-007 | MasterPage 파트 루트는 `hm:masterPage`(마스터페이지 네임스페이스 접두) | `error__20250808__...hwpx`(vendored) `Contents/masterpage0.xml` 루트가 `xmlns:hm` 선언은 갖되 그 자신에는 접두를 안 붙인 **맨 `masterPage`**. 자식(`hp:subList` 등)은 정상 접두. 완전성 감사 census의 `corpusUnnamespacedElements` 발견과 일치. **6.4 트레인⑮에서 110건 추가 교차검증**(서로 다른 실문서 계열, 사설) — 전량 같은 무네임스페이스 루트 | `hwpx.oxml.master_page.parse_master_page`/`MasterPage`(`HwpxOxmlMasterPage.to_model()`)가 로컬명만으로 루트 검증(`local_name(node) == "masterPage"`, 네임스페이스 무관하게 설계) — 이 관찰이 애초에 그렇게 설계한 이유다 | `probes/dev007_masterpage_unnamespaced_root.py` | implemented (승격, 6.4 트레인⑮, 커밋 1e9c0c8 — 구조화 읽기 모델 추가) |
| DEV-008 | `hp:newNum`(`hp:autoNum`과 `AutoNumNewNumType` 공유)의 `xs:sequence`가 `autoNumFormat` 자식을 필수 선언(`ParaList XML schema.xml:2741-2759`) | 도달 가능한 vendored fixture 전수의 `hp:newNum`이 `num`/`numType`만 갖고 self-close — `autoNumFormat` 자식 0건. `numType`은 전량 `PAGE`(`reader_writer__PageFunctions.hwpx` 포함) | `HwpxOxmlParagraph.add_new_num`(`oxml/paragraph.py`, 6.2 트레인⑨)가 자식 없이 self-close 방출 — 스키마의 명목상 필수보다 실측 형태 우선 | `probes/dev008_newnum_autonumformat_omitted.py` | implemented (6.2 트레인⑨, 커밋 300ef8d) |
| DEV-009 | `hh:memoPr/@id`는 `xs:nonNegativeInteger`(0 유효)로 시작 관행 미명시. `@memoType` 열거값은 스키마가 `"NOMAL"`로 선언(`Header XML schema.xml:1740-1752`) | vendored `hh:memoPr` 전량 id가 **1부터** 시작(0 미관측 — `hh:borderFill`의 0-시작 관행과 이원). `memoType`은 관측된 값 전량 스키마 그대로 `"NOMAL"`(`"NORMAL"` 0건). 같은 소스의 재저장본(`error__20251107__test_re.hwpx`)은 `memoType` 속성 자체를 잃음(DEV-010의 재저장 손실 발견과 상응) | `_allocate_memo_shape_id`가 `_allocate_border_fill_id`의 0-시작 로직을 그대로 안 쓰고 1부터 시작. `_normalize_memo_shape_spec`의 `memo_type` 기본값이 스키마 철자 `"NOMAL"` 그대로(오타로 보여도 "정정" 안 함) | `probes/dev009_memopr_id_and_spelling_conventions.py` | implemented (6.2 트레인⑧, 커밋 530d75a) |
| DEV-010 | `hp:markpenBegin`/`markpenEnd`는 `hp:t`의 mixed-content 자식으로만 선언(`ParaList XML schema.xml:228-237`) | **증거가 hwpxlib 자체 error 회귀 fixture라는 점을 명시** — `error__20251107__test.hwpx`(vendored)에서 `markpenBegin`이 `hp:t` 밖, `hp:run` 직속(`hp:ctrl`의 형제)으로 스키마 위반 배치. 같은 소스의 재저장본(`_re.hwpx`)은 배치는 유지한 채 `color` 속성을 잃음 | `hwpx._document.highlight._span_highlights`(6.2 트레인⑥)는 `hp:t` 안에서만 markpenBegin/End를 짝짓는다 — 이 파일은 정직하게 0건 보고(조작 없음), `test_real_corpus_malformed_pair_reads_as_empty_not_fabricated`로 고정 | `probes/dev010_markpen_malformed_placement_and_resave_loss.py` | implemented (6.2 트레인⑥, 커밋 e1fad75) |
| DEV-011 | `ParameterList`(`ParaList XML schema.xml:2764`)는 재사용 복합타입으로만 선언 — 필드 클릭 액션(`hp:fieldBegin`)의 `hp:parameters`로만 스키마상 도달 가능. `unsignedintegerParam`은 어느 스키마 파일에도 없다(`integerParam`만 선언) | `error__20230809__test.hwpx`(vendored) `hp:rect` 도형의 마지막 자식이 `<hp:parameterset cnt="1" name="539"><hp:listParam cnt="1" name="12291"><hp:unsignedintegerParam name="28673">2</hp:unsignedintegerParam></hp:listParam></hp:parameterset>` — 같은 `ParameterList` 모양을 **다른 루트 태그**(`parameterset`, 필드용 `parameters`가 아님)와 **스키마 밖 잎 타입**으로 실어 도형 확장 속성에 붙인다. 47파일 코퍼스에서 유일 1건 | `hwpx.oxml.body.ParameterList`/`Parameter`가 `parameterset`/`parameters` 어느 루트든 `model.tag`로 왕복 보존, `unsignedintegerParam`을 관측 근거만으로 `_PARAM_LEAF_KINDS`에 등재(스키마 사본은 갱신 안 함 — 관측이 정본). **정정(2026-08 사이클 6.5 트레인⑰ 재검증)**: 이 "왕복 보존" 클래스 설계 자체는 정확하지만(직접 유닛테스트 `tests/test_coverage_promotion.py`가 증명), 실 문서를 여는 라이브 파이프라인은 이 클래스를 안 탄다 — `parse_preserved_element`(`body.py:817-846`)의 디스패치에 `parameterset`/`parameters` 분기가 없어, 실 `hp:rect`에 붙은 `hp:parameterset`은 (다른 손대지 않은 XML 서브트리 전부가 그렇듯) `GenericElement` 통과로 바이트 보존될 뿐 `ParameterList`로 읽히지 않는다 — `doc.shapes`도 읽기 접근자가 없다(쓰기 전용, `add_line` 등). `hp:parameters`(필드용) 쪽은 사정이 다르다: 실 저작 경로는 있으나 `ParameterList`가 아니라 `_document/fields.py`의 별도 속성-이름 기반 추출이고, `ParameterList`/`parameter_list_to_xml`를 실제로 호출하는 유일한 비-테스트 코드는 `scripts/generate_openrate_corpus_v7.py`(코퍼스 생성기 자신 — `doc.refs.add_hyperlink`로 만든 필드에 합성 파라미터를 직접 이어붙인다)뿐이다. 결론(당시): 원장의 `hp:parameterset` read=False는 정확한 판정이다(분류기 결함 아님) — 이 항목의 "implemented"는 클래스 설계·유닛테스트 수준의 정확성이지, 실 문서 파싱 배선의 정확성이 아니다. **배선 수리(2026-08 사이클 6.6 트레인⑳)**: 갭 지도 B.1이 이 재검증 자체를 다시 플래그해 코드를 다시 읽었다 — 트레인⑰의 판정이 정확했음을 재확인(`parse_preserved_element`에 분기 없음, `_preserved_element_to_xml`도 대응 분기 없어 만약 읽기만 고쳤다면 저장 시 `AttributeError`로 깨졌을 것). 두 디스패치 모두에 `parameterset`/`parameters` 분기를 추가(`body.py` `parse_preserved_element`/`_preserved_element_to_xml`) — 맹목적 전환이 아니라 로컬 코퍼스의 parameterset/parameters/listParam **306건 전수**(cnt 불일치 0건, cnt/name 밖 속성 0건)를 먼저 확인하고서. 원장의 `hp:parameterset` `codeRead`가 분류기 재실행만으로 자동으로 True로 바뀜(수동 오버라이드 불필요 — 정적 분석기가 새 분기를 실제 디스패치로 정확히 인식). 결함-부활 테스트: `tests/test_coverage_promotion.py::test_parameter_list_promoted_from_hwpxlib_sample_via_real_dispatch`(수리 전 되돌리면 재현 확인)+`test_parameter_list_sample_roundtrip_has_no_a1_loss`. | `probes/dev011_parameterset_schema_absence.py` | implemented (6.3 트레인⑪; 배선 수리 6.6 트레인⑳) — 클래스 설계·실 문서 오픈 경로 양방향 배선 모두 완료 |
| DEV-012 | `hp:line`(`LineType`)의 `startPt`/`endPt`는 `hc:PointType`(core), `hp:connectLine`(`ConnectLineType`)의 동명 필드는 `hp:ConnectPointType`(paragraph 자체 확장, `subjectIDRef`/`subjectIdx` 추가) — 스키마 자체가 두 타입을 다르게 선언(`ParaList XML schema.xml:2376-2377` vs `:2492-2493`), 로컬명만 보면 놓치기 쉽다 | `reader_writer__SimpleLine.hwpx`(vendored)는 `hc:startPt`/`hc:endPt`, `reader_writer__SimpleConnectLine.hwpx`·`error__20230818__test.hwpx`(둘 다 vendored)는 `hp:startPt`/`hp:endPt` — 스키마 그대로 실사용, 이원은 실제다 | `_create_line_element`(objects.py)가 `hc:` 하드코딩(이 필드쌍을 쓰는 유일한 저작 대상이라 분기 불필요). 읽기 쪽 범용 지오메트리 스캔(`_SHAPE_POINT_LOCAL_NAMES`, `HwpxOxmlShape.resize()`가 소비)은 로컬명만으로 매칭 — 네임스페이스 무관, 이미 양쪽 다 관용 | `probes/dev012_startpt_endpt_namespace_duality.py` | observed (대응 이미 충분 — 저작은 hc: 고정, 읽기는 로컬명 매칭) |
| DEV-013 | `ConnectLineType`은 `AbstractDrawingObjectType`을 `LineType`과 같은 방식으로 확장 — `offset`/`orgSz`/`curSz`/`scaMatrix`에 특별 제약이 없어 스키마만 보면 독립 도형처럼 보인다 | 유일한 정본 예시(`reader_writer__SimpleConnectLine.hwpx`, vendored)는 자유선이 아니라 `subjectIDRef`(→ 대상 도형의 `id`가 아니라 **`instid`**로 해석됨, 또 다른 함정)로 문서 내 다른 두 도형(rect·ellipse)을 잇는 "스마트 연결선": `offset`이 음수(부호 없는 32비트 직렬화, `4294966971`=`-325`)·`curSz`≠`orgSz`·`scaMatrix`가 평행이동까지 얹은 비항등 행렬. 다른 예시(`error__20230818__test.hwpx`, vendored, 미부착)는 `renderingInfo`에 `scaMatrix`가 **두 개** 들어있고 그중 하나가 `e1=0`인 퇴화(비가역) 행렬이라 정본 후보에서 제외 | 저작 API 미제공(`add_connect_line` 없음, 의도적 부재) — 읽기/보존만. 1개 정본 샘플로는 관계식(오프셋·스케일·이동을 원 도형 배치에서 유도하는 공식)을 역산할 근거가 없다는 게 명시적 결정 근거 | `probes/dev013_connectline_smart_connector_relationship.py` | deferred (6.4 트레인⑭, 커밋 2ed6df5 — 저작 보류) |
| DEV-014 | `hp:arc`(`ArcType`)는 좌표 3점(`center`/`ax1`/`ax2`, `hc:PointType`) + `type`(NORMAL/PIE/CHORD) 속성만 선언 — 각도/스윕 필드가 전혀 없다(비교: `EllipseType`은 같은 3점 + 스윕용 `start1`/`end1`/`start2`/`end2` 4점을 더 갖는다) | 유일한 실 예시(`reader_writer__SimpleArc.hwpx`, vendored)가 정확히 사분원 — `center`가 자기 bbox 모서리에 앉고 `ax1`이 바로 아래, `ax2`가 바로 오른쪽. `orgSz`가 그 3점만으로 정의되는 bbox와 정확히 일치 — 3점이 곧 전체 기하라는 스키마 자체를 실측이 뒷받침한다(임의 각도 지원 여부는 표본 1개로는 미확인) | `_create_arc_element`(objects.py, 6.4 트레인⑭)가 그 검증된 배치 하나만 점 단위로 저작하고, 나머지 세 모서리는 다른 도형이 이미 쓰는 `hp:flip` 미러링으로 얻는다(`corner` 인자 — 새 점 계산 없음) | `probes/dev014_arc_three_point_no_angle_contract.py` | implemented (6.4 트레인⑭, 커밋 2ed6df5) |
| DEV-015 | `version.xml` 파트 루트는 `version`(2024 초안 네임스페이스), 필수 속성명은 `targetApplication` | 벤더드 코퍼스 47/47이 루트 `hv:HCFVersion`(`http://www.hancom.co.kr/hwpml/2011/version`, 스키마와 다른 네임스페이스)이고, 필수 속성명이 47/47 오탈자 그대로 `tagetApplication`('r' 누락) — `targetApplication`(스키마 철자) 0건. DEV-007(masterpage.xml)과 같은 부류의 2024 초안-대-2011 실물 드리프트 | `hwpx.oxml.version_part.parse_hcf_version`/`HcfVersion`(`HwpxOxmlVersion.to_model()`, 6.4 트레인⑮)이 실 루트를 로컬명으로 검증하고, 오탈자 속성명을 필드명 `taget_application`으로 그대로 보존(정정하면 실 문서 속성명과 다시 멀어진다) | `probes/dev015_version_xml_root_and_spelling.py` | implemented (6.4 트레인⑮, 커밋 1e9c0c8) |
| DEV-016 | 없음 — 그룹 부재의 `offset`↔`transMatrix` e3/e6 동기, 부재 `id`의 문서-고유성은 스키마 어디에도 명시되지 않는다(둘 다 문법상 자유) | `hp:container` 74개(3파일) · 부재 203개 리버스: 부재 `offset`(x,y)이 자기 `renderingInfo/hc:transMatrix`의 이동성분(e3,e6)과 **수치로**(텍스트로는 아님) 항상 일치 — 32개 부재는 진짜 위치가 음수라 `offset`이 부호없는 32비트 랩어라운드로 직렬화되는데(DEV-013과 같은 인코딩, 다른 필드) `transMatrix`는 부호 있는 정수 그대로라 텍스트 비교는 오탐한다. 부재 `id`는 문서 전역이 아니라 작은 어휘(관측값 "0"·"2" 단 2종, 203개 중)만 씀 — 독립 도형의 `id`(문서-고유, `_object_id()`)와 다른 관습 | `_create_container_element`(objects.py, 6.5 트레인⑱)가 부재 재배치 시 `offset`과 `transMatrix` e3/e6을 항상 같이 갱신하고, 부재 `id`를 "0"으로 고정(관측 다수값과 일치)하며 `instid`만 고유하게 유지. 우리 저작 경로는 항상 bbox 최솟값 기준 평행이동이라 음수 오프셋(랩어라운드 절반)은 아직 미발생 — 실 문서에서만 관측 | `probes/dev016_container_member_offset_transmatrix_sync.py` | implemented (6.5 트레인⑱, 커밋 e0a2cb7) |
| DEV-017 | `hp:run`의 `RunType` 선택 그룹과 `hp:t`의 `TextType` 선택 그룹이 `lineBreak`/`hyphen`/`nbSpace`/`fwSpace`를 **둘 다** 선언 — hp:run 형제 배치와 hp:t 중첩 배치 둘 다 스키마상 허용, 스키마는 어느 쪽이 실사용인지 말하지 않는다 | 3원자 각 1파일(`error__20230818__test.hwpx`의 `lineBreak`, `error__20251107__test.hwpx`의 `fwSpace`, `error__20250808__...hwpx`의 `nbSpace`) 리버스: 전량 **단일 hp:t 안**에 중첩(`<hp:t>표기함<hp:lineBreak/>예)</hp:t>`류) — hp:run 형제 배치는 세 원자 어디서도 관측 0건. `hp:tab`이 쓰는 형제 배치(스키마도 허용·실사용도 그쪽)와 정반대 관습 | `_append_text_with_run_choice_atoms`(_document_primitives.py, 6.5 트레인⑲, `add_run(expand_special_characters=True)`가 호출)가 단일 hp:t 중첩 형태로 저작 — 기존 `_append_text_with_tabs`(hp:tab 전용, 형제 배치)는 손대지 않음 | `probes/dev017_run_choice_atoms_t_nesting_convention.py` | implemented (6.5 트레인⑲, 커밋 65406b7) |
| DEV-018 | 없음 — `hp:switch`/`hp:case`/`hp:default`는 벤더드 스키마 7종 어디에도 선언 자체가 없다(DEV-004/005류의 "스키마와 다르게 씀"이 아니라 "스키마가 아예 모름") | 실코퍼스 census(237파일)에서 236/237(99.6%) — 이 레지스트리가 찾은 것 중 코드 지원 0인 요소로는 최고 빈도. `hh:paraPr`(문단 스타일 정의) 안에서 같은 `hh:margin`/`hh:lineSpacing` 값 두 벌을 감싸는 버전호환 분기: `<hp:switch><hp:case hp:required-namespace="http://www.hancom.co.kr/hwpml/2016/HwpUnitChar">(2016값)</hp:case><hp:default>(2011값)</hp:default></hp:switch>` — OOXML의 `mc:AlternateContent`/`Choice`/`Fallback`과 같은 목적. `hp:case`의 유일한 속성 `hp:required-namespace`는 그 자체가 이례적 — 이 스키마 계열 속성은 보통 접두 없이 쓰는데(`id`·`zOrder` 등) 이건 속성 이름 자체에 `hp:` 접두가 붙는다 저작 정확성 버그 아님 — `header_part.py`의 `_apply_paragraph_margins`/`_apply_paragraph_line_spacing`이 이미 `hh:paraPr`의 **전 자손**(직속 자식만이 아니라)을 훑어서 갱신하므로, 실 Hancom 문서의 스타일을 우리가 편집해도 `hp:case`·`hp:default` 두 사본이 같이 갱신됨(코드 읽어서 확인·프로브가 실행으로 재확인). **읽기 모델 수리(2026-08 사이클 6.6 트레인㉑)**: 저작 경로와 별개로, `header.py`의 **타입 있는 읽기 모델**(`ParagraphProperty`, 편집용 살아있는 트리와는 다른 스냅샷 dataclass)은 `parse_paragraph_property`가 `hh:paraPr`의 직속 자식만 훑어 `hp:switch`를 통째로 `other_children`(불투명)으로 넘겨서, `.margin`/`.line_spacing`이 236/237 실문서 전부에서 조용히 `None`이었다 — 실측(`tool__blank.hwpx`, 16/16 paraPr) 확인. `ParagraphPropertyVersionBranch`/`ParagraphPropertyVersionSwitch` 신설(`hp:required-namespace` 노출·`case`/`default` 양쪽 독립 접근) + `parse_paragraph_property`가 직속 자식이 없을 때 `case`(우선)·`default`(폴백) 값으로 최상위 `margin`/`line_spacing`을 채우도록 배선. **읽기 전용** — 저작 API·직렬화기(`*_to_xml`) 없음(팀장 지시대로, 저작 경로는 이미 안전하므로). 실 소비자(`doc.styles.paragraph_property`, `form_fit.measure`)가 자동으로 혜택. 원장 `codeRead`가 분류기 재실행만으로 switch/case/default 셋 다 True로 전환(수동 오버라이드 불필요). 결함-부활: 되돌린 `parse_paragraph_property`로 16/16 margin=None 재현 확인 후 복원 | `probes/dev018_switch_case_default_schema_absence.py` | implemented (읽기 모델, 6.6 트레인㉑) — 저작 경로는 여전히 API 없이 안전(의도적, 팀장 지시), 읽기만 1급 승격 |
| DEV-019 | `ParaPrType`(`ParaList XML schema.xml`)의 `xs:sequence`는 `align`/`heading`/`breakSetting`/`autoSpacing`/`margin`/`lineSpacing`/`border`를 같은 자격의 형제 자식으로 나열 — 스키마는 `autoSpacing`을 `margin`/`lineSpacing`과 다르게 취급할 근거를 전혀 안 준다 | 실코퍼스(47파일) `hh:autoSpacing` 1832건 **전량 `hh:paraPr`의 직속 자식**(hp:switch 중첩 0건) — 같은 `hh:paraPr` 안에서 DEV-018의 `hh:margin`/`hh:lineSpacing`(3534건 hp:switch 중첩·47건만 직속)과 **정반대** 관행. 6.6 트레인㉓의 `apply_paragraph_auto_spacing` 1차 설계는 margin/lineSpacing과의 유추로 autoSpacing도 switch 중첩일 거라 가정하고 문서화했으나, 코드 배포 전 실측으로 틀렸음을 확인 | `_apply_paragraph_margins`/`_apply_paragraph_line_spacing`과 같은 자손-순회 관용구를 재사용하되(방어적, 비용 0), 신규 생성 시 `hh:autoSpacing`을 직속 자식으로 둔다(관측과 일치) — `oxml/header_compat.py`. 독스트링은 "확인한 사실"만 주장하도록 수정(가정 문구 제거) | `probes/dev019_autospacing_switch_non_nesting.py` | implemented (6.6 트레인㉓, 정리 트레인에서 등재) |
| DEV-020 | `hh:layoutCompatibility`는 48개 플래그 이름 전부 선언(DEV-005가 읽기 쪽에서 이미 등재), `hh:compatibleDocument/@targetProgram`·`hh:docOption/hh:linkinfo/@path`는 스키마상 무제약 문자열 | 실코퍼스(47파일) 3중 확인: `targetProgram`은 `"HWP201X"` 47/47 유일값·`layoutCompatibility` 플래그 0개 47/47(DEV-005와 별개로 쓰기 쪽에서 재확인)·`linkinfo/path` 빈 문자열 47/47. 이 그룹에서 실제로 갈리는 속성은 `pageInherit` 하나뿐(8/47 `"1"`). 불리언은 전부 `"0"`/`"1"`(DEV-006과 같은 관례, `"true"`/`"false"` 0건) | 6.6 트레인㉓ `oxml/header_compat.py`의 `set_compatible_document_target_program`/`set_layout_compatibility_flags`/`set_doc_option_link_info`가 이 3중 확인된 계약대로 저작 — 값을 강제하지 않고(관측 밖 값의 실증 부재) 기본 생성 시 관측된 다수값을 채운다 | `probes/dev020_document_options_schema_wide_real_narrow.py` | implemented (6.6 트레인㉓, 정리 트레인에서 등재) |
| DEV-021 | 없음 — DEV-018과 같은 사유(`hp:switch`/`hp:case`/`hp:default` 스키마 미선언), 이번엔 `hh:paraPr` 밖의 두 번째 맥락에서 재확인 | `error__20230426__HwpxTest1.hwpx`(vendored) 하나가 `hp:run`(`hp:t`·`hp:switch`·`hp:t`) 직속 자식으로 `hp:switch`를 가짐 — `hp:case`(`hp:required-namespace="http://www.hancom.co.kr/hwpml/2016/ooxmlchart"`)는 `hp:chart`(2016 OOXML차트) 전체를, `hp:default`는 `hp:ole`(레거시 OLE) 전체를 감싼다. DEV-018(같은 속성값 두 벌)과 구조가 다르다 — **같은 개체의 서로 다른 표현 두 종**(최신 클라이언트=차트 직접 렌더, 구식=OLE 스냅샷 폴백)을 감싼다. OOXML Markup Compatibility와 같은 목적을 구조적으로 다른 방식으로도 씀을 확인 | 저작 정확성 버그 아님 — 미인식 run 자식은 기존 `PreservedElement` 폴백(`GenericElement`)이 불투명하게 바이트 보존(`roundtrip_report`로 무손실 확인). 차트/OLE 대안을 아는 타입 읽기 모델은 없음(우리 `add_chart` 저작 경로도 이 분기를 안 씀) — 다음 사이클 갭 지도 후보로 별도 기록(직접 수리 안 함, 이 사이클 범위 밖) | `probes/dev021_switch_as_inline_object_alternate.py` | observed (구조 확인·무손실 재확인, 타입 모델은 다음 사이클 후보) — 6.7 트레인㉗ 레지스트리 이식 조사가 같은 픽스처로 독립 재확인, add_chart 폴백 갭 결론 동일(중복 항목 없음, 상호 참조만) |
| DEV-022 | 없음 — DEV-018/021과 같은 사유. `hh:tabItem`의 `unit` 속성도 스키마 미선언(`pos`/`type`/`leader`만 선언) | `hh:tabPr`도 `hp:switch`로 감싸인다(449건, DEV-018의 세 번째 확인된 맥락). **DEV-018과 달리 두 분기 값이 다르다**: `hp:case`의 `hh:tabItem/@pos`가 `hp:default`의 **정확히 절반**(실코퍼스 34/34 쌍, 예외 0), `hp:case`만 `unit="HWPUNIT"`을 명시(449/449) — `hp:default`·직속(스위치 없는) `hh:tabItem`은 전혀 안 씀(0/483). `TabStop` 독스트링이 미리 남겨 둔 "unit 속성 드물게 관측, 원인 미확인"의 그 원인이 이거였다. 대조군(`error__20240626__no_manifest.hwpx`)의 직속 `hh:tabItem` 값(8064·3216)이 `hp:default` 스케일과 정확히 일치 — 어느 분기가 "진짜" 스케일인지 판정 근거 | `header.py`의 `TabDefinitionVersionBranch`/`TabDefinitionVersionSwitch` 신설, `parse_tab_definition`이 직속 `hh:tabItem`이 없을 때 `hp:default`를 대표값으로 채운다(DEV-018의 "case 우선"과 **반대** 선택 — 같은 값 두 벌이 아니라 다른 스케일 두 벌이므로). `ensure_tab_definition`의 dedupe 비교(`_tab_definition_matches`)도 같은 직속-자식-전용 사각지대를 공유해 기존 switch-감싼 tabPr과 동등한 스펙이 "불일치"로 오판돼 중복 생성되는 실결함이었다 — 같은 폴백으로 수리(결함-부활: 되돌리면 5/5 tab_stops 빈 값 + dedupe가 중복 tabPr 생성, 복원 후 실값 4/4 확인+중복 미생성 확인) | `probes/dev022_tabpr_switch_case_default_scale_mismatch.py` | implemented (6.7 트레인㉕) — 읽기 대표값 선택은 실측 근거(hp:default가 실 스케일), 양쪽 분기 모두 `version_switch`로 보존 |
| DEV-023 | 없음 — 스키마와 실측이 완전히 일치(이 레지스트리에서 드문 경우). `TableType` 시퀀스가 `hp:label`을 `minOccurs="0"`, 전체 `hp:tr` 뒤 마지막 위치로 선언, 속성 11종(`landscape`만 열거형 `WIDELY`/`NARROWLY`, 나머지는 전부 `xs:nonNegativeInteger`) | 이 레지스트리가 찾은 것 중 코드 지원 0인 요소로 최고 빈도권(census 모집단 237파일 중 64파일=27%, 벤더드 47파일엔 실물 0건이라 사설 코퍼스 리버스 필요 — `docs/_extra/element-census.json`의 populationNote가 이미 밝힌 두 번째 `--corpus` 루트, 경로는 명시하지 않음, 프라이버시). 사설 문서 75개·`hp:label` 436건 리버스: (a) 부모는 항상 `hp:tbl`(436/436), 항상 마지막 자식(스키마 시퀀스 위치와 일치) (b) 스키마 선언 11종 전부, 그 이상도 이하도 없음 (c) 실측 값 조합은 **정확히 2가지뿐** — 2열×9행 소형 라벨 시트(325건)·1열×2행 대형 정사각 명패(111건, `boxwidth==boxlength`) (d) `landscape="WIDELY"` 100%(`"NARROWLY"`는 스키마상 합법이나 실측 미확인) | `body.py`에 `Label`/`parse_label_element`/`_label_to_xml` 신설(DEV-011의 `ParameterList`와 같은 패턴으로 `parse_preserved_element` 디스패치에 배선). `HwpxOxmlTable.label`/`.set_label()`/`.remove_label()`로 저작 표면 추가 — 항상 마지막 자식으로 배치(스키마·실측 위치 일치), 관측된 2개 조합으로 값을 제한하지 않음(다른 조합의 한컴 수용 여부는 실증 근거 없음, 억측 안 함). 프로브는 사설 코퍼스를 재참조하지 않는다(설계상 — 벤더드 스키마 검증 + 합성 값 왕복 보존만 재현 가능) | `probes/dev023_label_avery_layout_schema_match.py` | implemented (6.7 트레인㉖) — 읽기+저작 모두. **Render-verified(6.8, 트레인㉘)**: openrate v11(트레인㉔가 배선한 요소-직접 경로로 원장 환류, `by-openrate-corpus`) 15/15 render-checked — 관측 2클러스터뿐 아니라 관측 밖 조합(3×6 그리드·`landscape="NARROWLY"`·partial 속성)도 전부 실한컴이 수용, 위 (d)의 "NARROWLY 실측 미확인"은 이제 "합법 선언값이며 최소 1개 합성 표본에서 한컴이 거부하지 않음"으로 갱신(실코퍼스에서의 사용 빈도 자체는 여전히 0 — 별개 사실) |
| DEV-024 | `ParaList XML schema.xml`(자신의 `targetNamespace`가 2024 `hp` 네임스페이스, 5행)이 도형 좌표류 `pt0`-`pt3`(사각형 모서리)·`center`/`ax1`/`ax2`(타원·호)를 **자신의 hp 타깃 네임스페이스에** 지역 선언하면서 전부 `type="hc:PointType"`으로 타입만 core에서 빌려 쓴다 — DEV-012(`startPt`/`endPt`의 hc:/hp: 이원)와 같은 클래스이나 그 필드쌍이 다루지 않는 나머지 좌표 패밀리 전체로 일반화한 별도 항목(DEV-012의 상위집합 아님 — DEV-012는 line/connectLine 이원 자체가 주제, 이 항목은 hc:-only 관행의 확장) | 벤더드 코퍼스 47파일 전수: `hc:pt0` 195건/17파일, `hp:pt0` 0건 — 스키마가 형식상 옳다고 볼 hp: 접두는 실측 0 | `objects.py`의 `_SHAPE_POINT_LOCAL_NAMES` 프로즌셋(주석: "namespace differs per shape type in real Hancom output")과 그 소비자(`resize()`/`_scale_geometry()`)가 로컬명 매칭이라 이미 흡수 — 사각형·타원 저작 라이브 재확인(직접 `HwpxDocument.new()` 저작 후 직렬화 바이트에서 hc:pt0/hc:center/hc:ax1 리터럴, hp: 접두 0건) | `probes/dev024_shape_point_family_core_namespace.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — DEV-012와 같은 로컬명-매칭 설계가 사각형·타원 좌표에도 적용됨을 재확인) |
| DEV-025 | `fillBrush`는 `ParaList XML schema.xml`(1037·2350행)과 `Header XML schema.xml`(471행) **양쪽에서 독립적으로** 지역 선언(둘 다 `type="hc:FillBrushType"`) — 형식상 `hp:fillBrush` 또는 `hh:fillBrush`가 가능하나 실문서는 셋 다 안 쓴다 | 벤더드 코퍼스 47/47 파일에서 `hc:fillBrush` 796건, `hp:`/`hh:` 접두 0건 | `_document_primitives.py`의 fillBrush 작성 경로가 이미 hc: 고정 — 채움색 있는 사각형 저작 라이브 재확인(직렬화 바이트에 hc:fillBrush만, hp:/hh: 0건) | `probes/dev025_fillbrush_core_namespace.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — DEV-024와 같은 클래스, 두 스키마 파일이 각자 선언하는데도 실측·우리 출력 모두 hc: 고정) |
| DEV-026 | `Header XML schema.xml`이 `<xs:element name="trackchangeConfig">`(정자)를 선언 | 벤더드 코퍼스 47/47 파일 `hh:trackchageConfig`(오자, n 탈락) 46파일·47건, 정자 0건 | `header_part.py`가 `f"{_HH}trackchageConfig"`로 오자를 그대로 방출(실호환성 유지 목적), `header.py`/`header_part.py`는 두 철자 다 인식하는 방어적 읽기 — 이미 실호환 전략이 구현돼 있다(DEV-004·DEV-009·DEV-038과 같은 오탈자-복제 클래스, 서브시스템만 다름) | `probes/dev026_trackchageconfig_typo.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — 오자 방출·양쪽 철자 방어적 읽기 모두 기존 코드) |
| DEV-027 | 없음 — `hp:lineseg`/`hp:linesegarray`(한글 줄배치 캐시)는 7개 스키마 파일 어디에도 선언되지 않는다 | 실코퍼스 census(237파일) `hp:lineseg`/`hp:linesegarray` 213/237(89.9%) — 이 레지스트리가 찾은 것 중 스키마 지원 0인 요소 중 최고 빈도권. 벤더드 47파일 중 45파일에 `hp:linesegarray` 실재. 캐시-겹침 인과관계는 `body_patch.py` 주석이 2026-07-07 AI중점학교 신청서 실측(교체 문단만 줄겹침, 캐시 제거된 클론 문단은 정상)을 직접 인용 — 렌더 결과 자체는 오라클 없이 이 세션에서 재관찰 불가, previously-verified로 인용만 | `patch.py`의 `_strip_paragraph_layout_cache`(`table_patch.py`/`body_patch.py`가 호출)가 이미 이 편차를 전제로 설계 — 합성 문단으로 실제 제거 동작 라이브 재확인 | `probes/dev027_lineseg_cache_undeclared.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — 캐시 무효화 인과는 previously-verified 인용, 구조·제거 함수는 이 프로브가 직접 재확인) |
| DEV-028 | `Header XML schema.xml`이 `hh:offset`(언어별 오프셋 -100~100, 문서화="언어별 오프셋. 단위는 %.")을 선언하나 부호가 위/아래 중 뭘 뜻하는지는 전혀 명시하지 않는다 | previously-verified — 정비 이력(2026-08-01 저작 충실도 수리): "첨자 offset 양수=아래(감사 오독 정정)", 이전 감사가 부호를 반대로 읽어 실한컴 렌더 재측정으로 정정한 이력. 이 세션에서 렌더 재확인은 불가(오라클 없음), 코드가 이미 인코딩한 관례를 라이브로 재확인 | `document_parts.py`의 `_run_style_script_matches`/`_run_style_apply_script_extension`가 `wanted_offset = "-30" if script == "sup" else "30"`으로 직접 인코딩, `error__20250808` 실코퍼스 실측(charPr id=513)에 따라 `hh:supscript`/`hh:subscript` 실요소도 병행 방출 — `doc.styles.ensure_run(script="sup"/"sub")` 라이브 재확인 | `probes/dev028_subscript_offset_sign_convention.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — 부호·병행 요소 방출 모두 기존 코드, 렌더 사실 자체는 previously-verified 인용) |
| DEV-029 | 도형류(`RectangleType`/`EllipseType`/`LineType`)는 `sz`/`orgSz`/`curSz`와 좌표 요소(`pt0`-`pt3` 등)를 형제로만 나열 — 어느 쪽이 렌더링 시 우선하는지 스키마는 명시하지 않는다 | previously-verified — 오너 메모리 `shape-authoring-namespace-defect.md`(5.0.0, 실한컴 7/7 검증)가 이 기하-우선 메커니즘을 실측. 이 세션에서 렌더 재확인은 불가, 구조(sz만 바꾸면 기하 불변·resize()는 기하까지 갱신) 대조는 라이브 재확인 | `objects.py`의 `resize()` docstring이 메커니즘을 직접 서술("Hancom renders ... from its type-specific geometry ... scaled by scaMatrix — not from sz") — sz만 편집 시 pt0-pt3 불변, `resize()` 호출 시 sz·기하 동시 갱신을 라이브로 대조 확인 | `probes/dev029_resize_geometry_over_sz.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — resize()가 이미 기하까지 갱신, 렌더 사실 자체는 previously-verified 인용) |
| DEV-030 | `hp:fieldBegin`/`hp:fieldEnd`는 `hp:run`의 `ctrl` 안에 오는 컨트롤일 뿐 — 하이퍼링크가 통상 3-run(시작 컨트롤·표시 텍스트·끝 컨트롤)로 나뉜다는 것도, 어느 run이 링크 charPr을 가져야 하는지도 스키마는 규정하지 않는다(구조 규칙, 렌더 무관이라 오라클 `not-applicable`) | 2026-08-01 수리 트레인의 한컴 gold 코퍼스 대조("파랑 전염" 회귀 재현·수정 이력)가 근거 — 표시 런만 링크 charPr을 갖고 나머지 두 컨트롤 런은 주변 charPr을 유지해야 뒤에 오는 문단이 링크 서식을 상속하지 않는다 | `paragraph.py`의 `add_hyperlink` 주석("링크 서식은 표시 텍스트 런에만 싣고 fieldBegin/fieldEnd 런은 주변 서식을 유지") — 라이브 하이퍼링크 저작 후 4-run 구조(빈 시작런·fieldBegin 컨트롤런·표시런·fieldEnd 컨트롤런) 확인, 컨트롤 런 2개는 주변 charPrIDRef 유지·표시 런만 요청한 charPrIDRef 획득을 재확인 | `probes/dev030_hyperlink_charpr_scope.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — add_hyperlink가 이미 이 관례로 저작) |
| DEV-031 | `ParaList XML schema.xml`이 `dirty` 속성을 범용 `xs:boolean`(기본값 false)으로 선언 — 언제 누가 소비하는지 스키마는 말하지 않는다 | previously-verified — M7 네이티브 목차 트레인 P3 실측 이력(2026-07-02): `refresh_document`(새로고침)와 render를 의도적으로 별도 세션으로 분리해 같은 세션 export 크래시를 피한 실측 이력. 크래시 재현은 위험해 이 세션에서 시도하지 않음(오라클도 없음) — dirty 세팅 자체의 구조만 라이브 재확인 | `toc_author.py`의 `mark_toc_dirty()`가 이 트리거를 캡슐화, 크래시 회피 책임은 호출자(automation 계층의 `MacHancomOracle.refresh_document` 분리 설계 이유) — `add_native_toc(dirty=True)`가 실제 `dirty="1"`을 설정하고 `mark_toc_dirty()`가 기존 문서에서도 재플래그함을 라이브 확인 | `probes/dev031_toc_dirty_next_open_trigger.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — dirty 세팅·재플래그 모두 기존 API, 크래시·재계산 타이밍 자체는 previously-verified 인용) |
| DEV-032 | `hp:p`는 `paraPrIDRef`/`styleIDRef`를 속성으로 가지나 "개요 레벨"이 TOC 수집 로직과 연결된다는 스키마 개념 자체가 없다 — 실제 수집 판단은 TOC 필드의 `hp:parameters`(DEV-016/DEV-037과 동형: 이름-값 목록, 개별 이름의 의미는 스키마 밖) 안 문자열 커맨드에 있다 | previously-verified — `toc_author.py`의 `add_native_toc` docstring이 "(measured)"로 직접 표시: 한컴이 개요 스타일 문단뿐 아니라 `ContentsStyles:wstring:0:` 커맨드를 통해 스타일 0(바탕글) 문단도 재계산 시 끌어온다. 실제 재계산 시 스타일 0 문단이 끌려오는지는 렌더 확인 필요(오라클 없어 이 세션에서 불가) — 우리 저작 경로가 이 커맨드 파라미터를 실제로 내는지는 라이브 재확인 | `add_native_toc`가 이 규칙을 알고 있다(생성 시 헤딩만 담아 회피) — 라이브 TOC 저작 후 직렬화 바이트의 Command 파라미터에 `ContentsStyles:wstring:0:` 리터럴이 실재함을 확인 | `probes/dev032_toc_contentsstyles_collection_command.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — 커맨드 파라미터 방출은 기존 코드, 재계산 시 실제 수집 동작 자체는 previously-verified 인용) |
| DEV-033 | 없음 — `hp:headerApply`/`hp:footerApply`(페이지 유형별 BOTH/EVEN/ODD 머리말·꼬리말 적용)는 7개 스키마 파일 어디에도 선언되지 않는다(`hp:header`/`hp:footer` 자체는 선언됨, `HeaderFooterType`) | **정직 갭** — 이번 체크아웃의 벤더드 47파일·현재 237파일 census(`docs/coverage-ledger.json`) 어디에도 표본 0건. 옛(별도 조사 당시) 모집단 스냅샷과 현재 모집단이 달라 재현 불가 — `section_format.py`의 상당한 기존 구현(빈손에서 나온 코드가 아님) 자체가 실사용 근거를 시사하나, 이번 등재는 그 실측을 재확인하지 못했음을 정직 표기한다(추정 금지) | `section_format.py`의 `_ensure_header_footer_apply`/`_apply_elements`/`_match_apply_for_element`가 이 미선언 메커니즘 위에 이미 구축 — `doc.page.set_header(page_type="EVEN"/"ODD")` 라이브 저작 후 서로 다른 `applyPageType` 값의 `hp:headerApply` 요소 실재를 직접 확인(코퍼스 표본이 없어 이 라이브 확인이 이 항목의 유일한 실측 근거) | `probes/dev033_header_footer_apply_undeclared.py` | observed (6.7 트레인㉗ 레지스트리 이식, 구조·저작 API 확인 완료, 실문서 빈도는 이번 체크아웃 모집단에서 미확인 — 정직 결측, 억측 안 함) |
| DEV-034 | 없음 — 메모(코멘트) 서브시스템(`hp:memo`/`hp:memogroup`/`hp:paraList`)이 7개 스키마 파일 어디에도 선언되지 않는다(`ParaListType`은 스키마 안에 있지만 소문자 요소 이름 `paraList` 자체는 없음) | 현재 237파일 census `hp:memo`/`hp:memogroup`/`hp:paraList` 각 24/237(10.1%) — 별도 조사 당시 옛 모집단(1파일뿐)보다 훨씬 강한 실측. 메모 렌더 정합성은 별도 스테이지(오너 메모리 `s058-s059-progress.md`, `MemoShapeIDRef` 버그 수정)에서 실한컴 검증 이력 있음 | `memo.py`가 `paraList`를 `p`와 함께 메모 본문의 유효 자식으로 로컬명 매칭 — `doc.notes.add_memo()` 라이브 저작 후 세 미선언 요소 전부 직렬화 바이트에 실재함을 확인 | `probes/dev034_memo_subsystem_undeclared.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — 미선언 요소 저작·읽기 모두 기존 코드, census 재확인으로 옛 조사보다 강한 근거 확보) |
| DEV-035 | 없음 — 바이너리(이미지 등) 첨부 매니페스트(`hh:binDataList`/`hh:binItem`)가 7개 스키마 파일 어디에도 선언되지 않는다 | **정직 갭** — DEV-033과 같은 사유로 이번 체크아웃의 벤더드 47파일·현재 237파일 census 어디에도 표본 0건(옛 모집단 스냅샷과 현재 모집단 불일치, 재현 불가). 그림 삽입 자체(`add_picture`/`media.add_image`)는 이미 `support-matrix.md`("그림 삽입/치환" 행)에서 실한컴 render-verified — 그 기능이 이 미선언 매니페스트 없이는 작동할 수 없다는 정황 근거는 있으나, 이번 등재가 직접 재확인한 것은 라이브 저작 결과뿐(추정 금지) | `header_part.py`의 바이너리 아이템 접근자(`_bin_data_list`, id 채번, 아이템 등록)가 이 미선언 구조 위에 이미 구축 — `doc.media.add_image()` 라이브 저작 후 header.xml에 두 요소 모두 실재함을 확인 | `probes/dev035_bindata_manifest_undeclared.py` | observed (6.7 트레인㉗ 레지스트리 이식, 구조·저작 API 확인 완료, 실문서 빈도는 이번 체크아웃 모집단에서 미확인 — 정직 결측, add_picture 자체는 이미 render-verified) |
| DEV-036 | OWPML 요소는 아니지만 우리 코드가 다루는 인접 OPC/매니페스트 계약 — `content.hpf`/`manifest.xml`은 통상 모든 part를 나열하는 게 원칙이나, 실한컴 산출물은 `version.xml`을 나열에서 뺀다. DEV-015(같은 파일의 루트 요소명·`tagetApplication` 오탈자)와는 다른 측면의 사실 — 중복 아님, 상호 참조만 | 라이브 재현 가능(코퍼스 불필요) — `HwpxDocument.new().to_bytes()`의 zip 안에 `version.xml`은 실재하나 `Contents/content.hpf`·`META-INF/manifest.xml` 어디에도 나열되지 않음을 직접 확인 | `opc/package.py`의 `version_path()`가 매니페스트 조회 실패 시 고정 경로(`VERSION_PATH`)로 폴백, 주석이 "실제 한컴 산출물은 version.xml을 manifest에 선언하지 않고 고정 경로로 둔다(정상 경로, debug 로그)"를 직접 명시 | `probes/dev036_version_xml_manifest_omission.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — 폴백 로직 기존 코드, DEV-015와 상호 참조) |
| DEV-037 | `hp:parameters`(`hp:ParameterList`)는 `stringParam`/`integerParam`/`listParam` 등 범용 이름-값 목록만 규정 — 개별 파라미터 이름("Command"/"Direction"/"HelpState")의 의미도, 그 문자열 내용 자체의 문법도 스키마 밖 | 라이브 재현 가능(코퍼스 불필요) — 누름틀 저작 후 `Command` stringParam이 `Clickhere:set:{len}:{payload}` 형태(중첩 `Direction:wstring:{len}:{...}`)임을 직접 확인. BMP 안 문자만 쓴 경우 길이 접두가 코드포인트 수·UTF-16 코드유닛 수 둘 다와 일치하지만, 서로게이트쌍이 필요한 문자(이모지)를 넣으면 둘이 갈린다 — 라이브로 재현: 길이 접두=2(파이썬 `len()`), 실제 UTF-16 코드유닛 수=3 | `paragraph.py`가 이 포맷을 직접 구현하며 docstring은 "Command lengths count UTF-16 characters"라 주장하지만 구현은 파이썬 `len()`(코드포인트 수)을 씀 — BMP 밖 문자가 누름틀 prompt/memo에 들어가면 길이 접두가 실제와 어긋나는 **미확인 경계 케이스**(수리 대상 아님, 실한컴 영수증 없이 "정답"을 억측하지 않음, 기록만) | `probes/dev037_clickhere_command_length_prefix_format.py` | observed (6.7 트레인㉗ 레지스트리 이식, BMP 케이스는 대응 이미 충분, BMP 밖 경계 케이스는 needs-probe로 남김 — 렌더/거부 여부 실증 부재) |
| DEV-038 | DEV-037과 같은 사유(`hp:parameters` 문자열 내용 무지) — 이 경우엔 파라미터 이름 자체가 스키마 밖에서 오탈자로 고정됨. 이 항목의 원 조사는 이를 "TOC 필드 파라미터"로 서술했으나, 실제로는 **CROSSREF** 필드(`add_page_crossref`)가 내는 파라미터임을 이번 재확인에서 정정(코드 재검증으로 발견) | `tests/fixtures/m7_toc_gold/`의 실한컴 gold pair 2개 section 파트에 `Fiexde` 리터럴 실재(스키마엔 이 이름의 "정자"조차 없어 대조 대상은 gold 코퍼스뿐) | `toc_author.py:21` 모듈 docstring: "The parameter spelling `Fiexde` replicates Hancom's own output verbatim" — 실제 방출 지점은 `add_page_crossref`(21행 아님, 355행: `_param(params, "booleanParam", "Fiexde", "1")  # sic`) — `add_page_crossref` 라이브 저작으로 재확인. DEV-004(trackchageConfig)와 같은 클래스이나 서브시스템·발견 경로가 달라 별도 항목 유지 | `probes/dev038_toc_fiexde_typo_replication.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — gold 코퍼스+라이브 저작 모두 확인, 소속 필드 정정) |
| DEV-039 | `hp:fieldBegin`의 `type` 속성은 단일 `hp:FieldType` enum이고 `dirty`는 모든 필드 타입이 공유하는 하나의 boolean 속성 — 필드 타입별로 "누가 언제 재계산하는지"가 다르다는 걸 스키마는 표현할 방법이 없다 | previously-verified — `toc_author.py:12-20` 모듈 docstring이 "P0 measured" 인용으로 CROSSREF는 "Hancom recomputes these automatically on edit/save"임을 명시(M7 P0 실측, 2026-07-02). 이 세션에서 렌더 재확인은 불가 — API·직렬화 결과의 비대칭 자체는 라이브 재확인 | `add_page_crossref`/`add_native_toc`가 이 비대칭을 이미 구분 구현 — API 시그니처 자체가 다름(`add_page_crossref`는 `dirty` 매개변수가 아예 없음, `add_native_toc`는 `dirty=True` 기본) + 라이브 저작 후 직렬화 결과에서 CROSSREF `dirty="0"`·TOC `dirty="1"`을 대조 확인(DEV-010/DEV-031의 TOC 쪽 재계산 타이밍 발견과 함께 봐야 할 짝 — 호출자가 이 비대칭을 모르면 실수하기 쉬워 `known-traps.md` 등재 이유) | `probes/dev039_crossref_vs_toc_recompute_asymmetry.py` | observed (6.7 트레인㉗ 레지스트리 이식, 대응 이미 충분 — API·직렬화 비대칭 모두 기존 코드, 재계산 타이밍 사실 자체는 previously-verified 인용) |
| DEV-040 | `Core XML schema.xml`이 `hc:MetaTagType`을 `<xs:complexType name="MetaTagType" mixed="true"/>`로 선언 — 자식·속성 0개, 문서화도 없는 완전히 빈 mixed-content 타입. 내용 구조에 대해 스키마가 줄 수 있는 정보가 사실상 0 | 벤더드 코퍼스 47파일에서 `<hh:metaTag>`/`<hp:metaTag>` 요소 실측 3건, 전부 **JSON 텍스트**(`{}`·`{"name":"#본문"}`·`{"name":""}`) — 비어있지 않을 땐 항상 `name` 키. **같은 로컬명이 별개 역할로도 쓰인다**: `hp:fieldBegin`의 `metaTag` **속성**(스키마 선언된 일반 문자열)은 6건 전부 빈 문자열 — 이건 요소가 아니라 속성, 혼동 주의. 신규문서 스켈레톤(`src/hwpx/data/Skeleton.hwpx`) 자체가 실한컴 산출 `<hh:metaTag>{"name":""}</hh:metaTag>`를 그대로 내장 | 요소 형태(`hh:metaTag`/`hp:metaTag`)는 타입 모델·저작 API 없음 — `GenericElement` 불투명 보존으로 왕복 무손실(3개 실 픽스처 전부 확인). 관측된 키가 `name` 하나뿐(3표본)이라 JSON 전체 어휘를 확신할 근거가 부족 — 타입 모델을 만들지 않는 게 의도적 보수(DEV-013류 근거-먼저 관행과 동형). `paragraph.py`의 필드 저작(`metaTag=""` **속성**)은 이미 실측과 일치(항상 빈 문자열) — 버그 아님 | `probes/dev040_metatag_json_mixed_content.py` | observed (6.7 정리 트레인, 갭 지도 v2 §D의 "hh:typeInfo·hh:metaTag 미조사" 후속 확인 — JSON 내용 규약은 신규 발견, 속성/요소 이원 구분과 함께 등재. **6.8 트레인㉘**: 같은 프로브가 짝 후보 `hh:typeInfo`도 정밀 판정 — 스키마·실물 편차는 없음(등재 대상 아님, 그 자체가 결과)이지만 `frozen-template` 분류가 정확하고 "저작 API 없음" 결정이 정당함을 `ensure_font`/`_build_font_element` 코드 확인 + 라이브 재현으로 확정. 부수 발견: 스켈레톤 상속 기본 폰트 2종은 typeInfo를 갖되(실한컴 원본에서 상속) `ensure_font()`로 신규 추가한 폰트는 전혀 안 가짐 — 정직한 비대칭, 수리 대상 아님(PANOSE류 서술자는 폰트 파일 자체의 속성이라 사용자가 임의로 타이핑할 근거가 없음) |
| DEV-041 | `ParaList XML schema.xml`의 `dutmal`(덧말) 요소가 `option`을 `xs:unsignedInt fixed="4"`(모든 유효 인스턴스가 반드시 4)로, `szRatio`를 `xs:positiveInteger`(1 이상만 허용)로 선언 | `tests/fixtures/hwpxlib_corpus/reader_writer__SimpleDutmal.hwpx`(hwpxlib 리더/라이터 왕복 픽스처, 실한컴 산출)의 유일한 실표본이 `option="0"`(4 아님)·`szRatio="0"`(1 미만, 스키마상 애초에 불법값)을 씀 — 두 스키마 주장 모두 실측이 정면으로 반증. 표본 1건뿐(빈도 낮음, macOS 편집기 메뉴 스캔은 1급 메뉴 항목으로만 확인 — 트레인㉙)이라 이 문서만의 우연인지 실제 관행인지는 모른다 | `oxml/body.py`의 `Dutmal`/`parse_dutmal_element`/`_dutmal_to_xml`이 실측값을 그대로 왕복 보존(강제 검증 없음). 저작 API `add_dutmal`(`oxml/dutmal_compose.py`, `doc.shapes.add_dutmal`)은 스키마의 그럴듯한 추정값(4/1 이상)이 아니라 이 유일한 실표본의 관측값(0/0)을 기본값으로 채택 — 틀린 추정을 신규 문서에 굳히는 것보다 유일한 실증을 충실히 재현하는 쪽을 택함 | `probes/dev041_dutmal_option_szratio_schema_mismatch.py` | observed (6.9 트레인㉞, 덧말·글자겹치기 저작 구현 중 발견 — 저작 API 설계에 직접 반영, 후속 실코퍼스 표본 확보 시 재검토 대상) |

**감사 갭 #12 재평가(보류 유지, 6.4 정리 트레인)**: `effects` 계열(glow/
reflection/softEdge/shadow/skew/scale/rgb 등, census 각 1파일)은 이번
사이클에서도 저작 API를 열지 않는다. 코퍼스 빈도(1/166)가 이 코퍼스의 관측
최하 컷이고, 같은 컷의 다른 항목(connectLine 2·라디오 2)도 이번 사이클 보류
결정을 받았다 — 완전성 감사 자신도 "후순위 타당"이라 판정한 자리다. 별도
리버스 없이 보류 사실만 여기 기록한다.

각 편차는 `증거:` 또는 표의 실측 칸으로 코퍼스, 캡처, 또는 재현 파일 경로를
명시한다. 확정 편차를 `_schemas`에 반영하면 관련 패치 커밋을 상태 칸에
남긴다. 프로브는 모두 `python probes/devNNN_*.py`로 단독 실행 가능하며,
근거 파일이 로컬에 없으면(`.gitignore` 대상 등) SKIP으로 종료한다(exit 0).
