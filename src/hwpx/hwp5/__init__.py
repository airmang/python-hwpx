# SPDX-License-Identifier: Apache-2.0
"""HWP 5.0 (``.hwp``) binary documents.

The modules here convert between an HWP 5.0 compound file and the OWPML parts
of an HWPX package, so :class:`hwpx.HwpxDocument` can open and save ``.hwp``
with the same document model it uses for ``.hwpx``.
"""
